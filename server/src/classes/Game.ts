import { IBoard, IGame, IPlayer } from "@types"
import Board from './Board'
import Player from './Player'

class Game implements IGame {
	// Creates a 10x10 board on initialization to use (could be UI generated by size choice)
	public board: IBoard = new Board()
	public player: IPlayer
	public opponent: IPlayer

	constructor() {
		// Setup player (could be UI generated)
		const player = new Player('Player', this.board)
		const opponent = new Player('Opponent', this.board)
		this.player = player
		this.opponent = opponent
	}

	public play(): void {
		console.log("Let's play battleship.")
	}
}

export default Game
